import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

// example of html code in js file
// only do once for root
ReactDOM.render(<App />, document.getElementById('root'));
